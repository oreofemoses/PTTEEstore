/* This file is no longer needed and can be considered for deletion by the user if they manage their project files manually. Hostinger Horizons cannot delete files. */
import React from 'react';

const DummyThemeContext = () => {
  return null;
};

export default DummyThemeContext;